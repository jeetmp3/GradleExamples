task callMe 

callMe << {
	println "Executing callMe"
}

callMe.onlyIf {
	return (params == 'true')
}

task compile << {
	if(callMe.didWork) {
		println "Now compiling files"
	}
}

defaultTasks = ['callMe']
